spring:
  datasource:
    url: jdbc:mysql://database:3306/test_db?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: web
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace


jwt:
  secret:
    key: x!A%D*G-KaPdSgVkYp3s5v8y/B?E(H+M
    jwt:
      token-validity-in-seconds: 3